Bootstrap: debootstrap
MirrorURL: http://archive.ubuntu.com/ubuntu/
OSVersion: xenial
Include: software-properties-common
%post
    add-apt-repository -s ppa:kernsuite/kern-1
    apt-add-repository multiverse
    apt-get update
    apt-get install -y lofar
